import { Link } from 'react-router-dom'
import { Button } from '@mui/material'

const Channel = () => {
  return (
    <Link to={'/'}>
      <Button>Main</Button>
    </Link>
  )
}

export default Channel